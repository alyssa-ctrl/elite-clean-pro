---
layout: base.njk
title: About | Elite Clean Pro · Calgary
description: 'Why Elite Clean Pro exists — the founding story of Calgary''s system-driven cleaning company built on SaaS-style operations and published commitments.'
ogTitle: About Elite Clean Pro | Calgary Cleaning
ogDescription: 'Elite Clean Pro is Calgary''s system-driven cleaning service — built on the 100-point system, A+ trained teams, and BioSmart Technology.'
ogImage: /og-image.jpg
ogType: website
extraHead: |
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "AboutPage",
    "url": "https://elitecleanpro.ca/about",
    "name": "About Elite Clean Pro",
    "description": "The founding story of Elite Clean Pro, a Calgary cleaning company built on SaaS-style operations.",
    "inLanguage": "en-CA",
    "mainEntity": {
      "@type": "LocalBusiness",
      "name": "Elite Clean Pro",
      "url": "https://elitecleanpro.ca/",
      "address": {
        "@type": "PostalAddress",
        "addressLocality": "Calgary",
        "addressRegion": "AB",
        "addressCountry": "CA"
      },
      "areaServed": [
        { "@type": "City", "name": "Calgary" },
        { "@type": "City", "name": "Airdrie" },
        { "@type": "City", "name": "Cochrane" }
      ],
      "foundingDate": "2026",
      "foundingLocation": "Calgary, AB"
    }
  }
  </script>
  <script type="application/ld+json">
  {"@context":"https://schema.org","@type":"BreadcrumbList","itemListElement":[
    {"@type":"ListItem","position":1,"name":"Home","item":"https://elitecleanpro.ca/"},
    {"@type":"ListItem","position":2,"name":"About","item":"https://elitecleanpro.ca/about"}
  ]}
  </script>
  <script type="application/ld+json">
  {"@context":"https://schema.org","@type":"WebPage","url":"https://elitecleanpro.ca/about",
  "speakable":{"@type":"SpeakableSpecification","cssSelector":[".hero-lede",".faq-a",".faq-a-inner"]}}
  </script>
---

<style>
/* ════════ TICKER STRIP (unified with pricing) ════════ */

/* SKIP LINK */

/* ════════ NAV (unified with pricing) ════════ */

/* ════════ NAV ════════ */

/* ════════ HERO — centered editorial ════════ */
.hero {
  text-align: center;
  padding: 7rem 1.75rem 5rem;
  max-width: 900px;
  margin: 0 auto;
}
.hero-eyebrow {
  font-family: var(--mono);
  font-size: 0.7rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--ink-soft);
  margin-bottom: 2rem;
}
.hero-eyebrow .teal { color: var(--teal); }
.hero-title {
  font-family: var(--serif);
  font-size: clamp(3rem, 7vw, 5.5rem);
  font-weight: 500;
  line-height: 0.98;
  letter-spacing: -0.035em;
  margin-bottom: 2rem;
}
.hero-title em { font-style: italic; color: var(--teal); font-weight: 400; }
.hero-lede {
  font-family: var(--serif);
  font-size: clamp(1.15rem, 1.5vw, 1.35rem);
  font-style: italic;
  font-weight: 300;
  line-height: 1.5;
  color: var(--ink-soft);
  max-width: 38rem;
  margin: 0 auto;
}
.hero-lede em { font-style: normal; color: var(--teal); }

/* ════════ STORY — first-person narrative ════════ */
.story {
  padding: 5rem 1.75rem 7rem;
  max-width: 760px;
  margin: 0 auto;
  border-top: 1px solid var(--rule);
}
.story-eyebrow {
  font-family: var(--mono);
  font-size: 0.7rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--muted);
  text-align: center;
  margin-bottom: 3rem;
  padding-top: 3rem;
}
.story-eyebrow .teal { color: var(--teal); }
.story-text {
  font-family: var(--serif);
  font-size: 1.35rem;
  line-height: 1.55;
  color: var(--ink);
  letter-spacing: -0.01em;
  margin-bottom: 1.5rem;
  font-weight: 400;
}
.story-text:first-of-type::first-letter {
  font-family: var(--serif);
  font-size: 4.5rem;
  font-style: italic;
  color: var(--teal);
  float: left;
  line-height: 0.85;
  margin-right: 0.6rem;
  margin-top: 0.4rem;
  font-weight: 500;
}
.story-text em {
  font-style: italic;
  color: var(--teal);
  font-weight: 400;
}
.story-text strong {
  font-weight: 500;
  color: var(--ink);
}

/* ════════ SEVEN OBSERVATIONS ════════ */
.observations {
  padding: 7rem 1.75rem;
  max-width: 1200px;
  margin: 0 auto;
  border-top: 1px solid var(--rule);
  background: var(--paper-deep);
}
.section-head-2col {
  display: grid;
  grid-template-columns: 0.7fr 2fr;
  gap: 3rem;
  margin-bottom: 4rem;
  align-items: start;
}
.section-num {
  font-family: var(--mono);
  font-size: 0.7rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--muted);
  padding-top: 0.8rem;
}
.section-num .teal { color: var(--teal); }
.section-title {
  font-family: var(--serif);
  font-size: clamp(2.3rem, 5vw, 4rem);
  font-weight: 500;
  line-height: 0.98;
  letter-spacing: -0.025em;
  margin-bottom: 1.25rem;
}
.section-title em { font-style: italic; color: var(--teal); font-weight: 400; }
.section-lede {
  font-family: var(--serif);
  font-size: 1.25rem;
  font-style: italic;
  font-weight: 300;
  color: var(--ink-soft);
  max-width: 38rem;
  line-height: 1.45;
}

.obs-list {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1rem;
}
.obs {
  background: var(--bone);
  border: 1px solid var(--rule);
  border-radius: 12px;
  padding: 1.5rem 1.75rem 1.5rem;
  display: flex;
  gap: 1.25rem;
  align-items: flex-start;
}
.obs-num {
  font-family: var(--serif);
  font-style: italic;
  font-size: 2rem;
  font-weight: 500;
  color: var(--teal);
  letter-spacing: -0.02em;
  line-height: 1;
  flex-shrink: 0;
  min-width: 2.5rem;
}
.obs-
.obs-title {
  font-family: var(--serif);
  font-size: 1.2rem;
  font-weight: 500;
  letter-spacing: -0.01em;
  line-height: 1.1;
  margin-bottom: 0.5rem;
  color: var(--ink);
}
.obs-text {
  font-size: 0.95rem;
  color: var(--ink-soft);
  line-height: 1.55;
}

.obs-link {
  margin-top: 3rem;
  text-align: center;
  font-family: var(--serif);
  font-style: italic;
  font-size: 1.15rem;
  color: var(--ink-soft);
}
.obs-link a {
  color: var(--teal);
  text-decoration: none;
  font-style: normal;
  font-family: var(--mono);
  font-size: 0.78rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  border-bottom: 1px solid var(--teal-line);
  padding-bottom: 0.15rem;
  margin-left: 0.6rem;
}
.obs-link a:hover { color: var(--ink); border-color: var(--ink); }

/* ════════ PHOTO PLACEHOLDER ════════ */
.photos {
  padding: 7rem 1.75rem;
  max-width: 1200px;
  margin: 0 auto;
  border-top: 1px solid var(--rule);
}
.photos-grid {
  display: grid;
  grid-template-columns: 2fr 1fr;
  grid-template-rows: auto auto;
  gap: 1rem;
}
.photo-slot {
  background: var(--bone);
  border: 2px dashed var(--rule);
  border-radius: 12px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 2rem;
  text-align: center;
  gap: 0.75rem;
  color: var(--muted);
  min-height: 240px;
}
.photo-slot.large {
  grid-row: span 2;
  min-height: 500px;
}
.photo-slot-tag {
  font-family: var(--mono);
  font-size: 0.7rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--teal);
  font-weight: 500;
}
.photo-slot-label {
  font-family: var(--serif);
  font-size: 1.3rem;
  font-style: italic;
  color: var(--ink-soft);
  letter-spacing: -0.01em;
  max-width: 18rem;
  line-height: 1.3;
}
.photo-slot-spec {
  font-family: var(--mono);
  font-size: 0.65rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--muted);
  margin-top: 0.5rem;
}

.photos-note {
  margin-top: 2rem;
  font-family: var(--mono);
  font-size: 0.72rem;
  letter-spacing: 0.10em;
  text-transform: uppercase;
  color: var(--muted);
  text-align: center;
}

/* ════════ WHAT WE BELIEVE ════════ */
.beliefs {
  padding: 7rem 1.75rem;
  max-width: 1200px;
  margin: 0 auto;
  border-top: 1px solid var(--rule);
  background: var(--paper-deep);
}
.beliefs-list {
  display: flex;
  flex-direction: column;
}
.belief {
  display: grid;
  grid-template-columns: 0.85fr 1.15fr;
  gap: 4rem;
  padding: 2.5rem 0;
  border-top: 1px solid var(--rule);
  align-items: start;
}
.belief:last-child { border-bottom: 1px solid var(--rule); }
.belief-left { padding-top: 0.3rem; }
.belief-num {
  font-family: var(--mono);
  font-size: 0.68rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--teal);
  margin-bottom: 0.85rem;
}
.belief-name {
  font-family: var(--serif);
  font-size: 1.75rem;
  font-weight: 500;
  line-height: 1.05;
  letter-spacing: -0.015em;
}
.belief-name em { font-style: italic; color: var(--teal); font-weight: 400; }
.belief-
.belief-body strong {
  font-weight: 500;
  color: var(--ink);
}

/* ════════ WHERE WE ARE ════════ */
.current {
  padding: 7rem 1.75rem;
  max-width: 900px;
  margin: 0 auto;
  border-top: 1px solid var(--rule);
  text-align: center;
}
.current-eyebrow {
  font-family: var(--mono);
  font-size: 0.7rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--teal);
  margin-bottom: 2rem;
}
.current-title {
  font-family: var(--serif);
  font-size: clamp(2rem, 4vw, 3rem);
  font-weight: 500;
  line-height: 1.05;
  letter-spacing: -0.025em;
  margin-bottom: 2rem;
}
.current-title em { font-style: italic; color: var(--teal); font-weight: 400; }
.current-text {
  font-family: var(--serif);
  font-size: 1.25rem;
  font-style: italic;
  color: var(--ink-soft);
  line-height: 1.55;
  max-width: 38rem;
  margin: 0 auto 2rem;
  font-weight: 300;
}
.current-text em { font-style: normal; color: var(--teal); font-weight: 400; }
.current-strip {
  display: flex;
  justify-content: center;
  gap: 3rem;
  flex-wrap: wrap;
  margin-top: 3rem;
  padding-top: 3rem;
  border-top: 1px solid var(--rule);
}
.current-stat {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.4rem;
}
.current-stat-label {
  font-family: var(--mono);
  font-size: 0.62rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--muted);
}
.current-stat-value {
  font-family: var(--serif);
  font-style: italic;
  font-size: 1.5rem;
  color: var(--ink);
  font-weight: 500;
  letter-spacing: -0.015em;
}
.current-stat-value .teal { color: var(--teal); }

/* ════════ CTA ════════ */
.cta {
  padding: 8rem 1.75rem 9rem;
  max-width: 1100px;
  margin: 0 auto;
  border-top: 1px solid var(--rule);
  text-align: center;
}
.cta-eyebrow {
  font-family: var(--mono);
  font-size: 0.7rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--ink-soft);
  margin-bottom: 2rem;
}
.cta-title {
  font-family: var(--serif);
  font-size: clamp(2.5rem, 5vw, 4rem);
  font-weight: 500;
  line-height: 1;
  letter-spacing: -0.025em;
  margin-bottom: 1.5rem;
}
.cta-title em { font-style: italic; color: var(--teal); font-weight: 400; }
.cta-sub {
  font-family: var(--serif);
  font-size: 1.2rem;
  font-style: italic;
  color: var(--ink-soft);
  max-width: 36rem;
  margin: 0 auto 3rem;
  line-height: 1.5;
}
.cta-actions { display: inline-flex; gap: 0.85rem; flex-wrap: wrap; justify-content: center; }
.btn {
  font-family: var(--mono);
  font-size: 0.78rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  text-decoration: none;
  border-radius: 999px;
  padding: 0.85rem 1.4rem;
  transition: all 0.18s ease;
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
}
.btn-primary { background: var(--ink); color: var(--paper); border: 1px solid var(--ink); }
.btn-primary:hover { background: var(--teal); border-color: var(--teal); color: var(--ink); }
.btn-ghost { background: transparent; color: var(--ink); border: 1px solid var(--rule); }
.btn-ghost:hover { border-color: var(--ink); }

/* ════════ FOOTER ════════ */

/* ════════ MOBILE ════════ */
/* Mobile nav hamburger — hidden on desktop, shown <=1024px */
.nav-toggle-input { display: none; }
.nav-toggle { display: none; }

@media (max-width: 1024px) {
  .nav-toggle {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 44px;
    height: 44px;
    cursor: pointer;
    margin-right: 0.5rem;
    flex-shrink: 0;
  }
  .nav-toggle-line {
    display: block;
    width: 22px;
    height: 1.5px;
    background: var(--ink);
    margin: 0 auto;
    transition: transform 0.22s ease, opacity 0.18s ease;
  }

  .nav-toggle-input:checked ~ 
  .nav-toggle-input:checked + .nav-toggle .nav-toggle-line:nth-child(1) {
    transform: translateY(6.5px) rotate(45deg);
  }
  .nav-toggle-input:checked + .nav-toggle .nav-toggle-line:nth-child(2) {
    opacity: 0;
  }
  .nav-toggle-input:checked + .nav-toggle .nav-toggle-line:nth-child(3) {
    transform: translateY(-6.5px) rotate(-45deg);
  }
  .section-head-2col { grid-template-columns: 1fr; gap: 1rem; }
  .section-num { padding-top: 0; }
  .obs-list { grid-template-columns: 1fr; }
  .photos-grid { grid-template-columns: 1fr; grid-template-rows: auto; }
  .photo-slot.large { grid-row: auto; min-height: 320px; }
  .belief { grid-template-columns: 1fr; gap: 1.25rem; padding: 2rem 0; }
  
}
@media (max-width: 640px) {

  .hero { padding: 4rem 1rem 3rem; }
  .hero-title { font-size: clamp(2.5rem, 12vw, 4rem); }
  .story { padding: 3rem 1rem 4rem; }
  .story-text { font-size: 1.15rem; }
  .story-text:first-of-type::first-letter { font-size: 3.5rem; }
  .observations, .photos, .beliefs, .current, .cta { padding: 4rem 1rem; }
  .obs { padding: 1.25rem 1.5rem; gap: 1rem; }
  .obs-num { font-size: 1.7rem; min-width: 2rem; }
  .photo-slot { min-height: 200px; padding: 1.5rem; }
  .photo-slot.large { min-height: 280px; }
  .belief-name { font-size: 1.5rem; }
  .current-strip { gap: 1.75rem; flex-direction: column; padding-top: 2rem; margin-top: 2rem; }
  .cta-actions { flex-direction: column; align-items: stretch; }
  .cta-actions .btn { justify-content: center; }

}

/* ═══════════════════════════════════════════════════════════
   UNIFIED MOBILE MENU — standardized across all pages
═══════════════════════════════════════════════════════════ */

@media (max-width: 1024px) {

}
/* ═══════════════════════════════════════════════════════════ */

/* ════════ FOOTER (unified with pricing) ════════ */

.footer-col p { font-family: var(--sans); font-size: 0.9rem; color: var(--ink-soft); line-height: 1.55; margin-bottom: 0.4rem; }
</style>

<!-- TICKER -->
<!-- ════════ NAV ════════ -->

<!-- UNIFIED MOBILE MENU -->



<!-- ════════ HERO ════════ -->
<section class="hero">
  <div class="hero-eyebrow"><span class="teal">About</span> · Why this exists</div>
  <h1 class="hero-title">Cleaning, <em>engineered.</em></h1>
  <p class="hero-lede">Elite Clean Pro is what happens when someone who sells property management software for a living gets tired of hearing landlords complain about their cleaning vendors — and decides to <em>build a real one.</em></p>
</section>

<!-- ════════ STORY ════════ -->
<section class="story">
  <div class="story-eyebrow"><span class="teal">01</span> · The story</div>

  <p class="story-text">
    I work in sales leadership at a property management software startup in San Francisco. <strong>The customers I sell to every day are landlords and property managers</strong> — the same people who hire cleaning services for unit turnovers, lobby maintenance, post-construction sites, and recurring office work.
  </p>

  <p class="story-text">
    PropTech has spent the last decade transforming property management. Pipelines documented. SLAs published. Named account managers. Real reporting. The operational discipline that lets a software company answer "where does this stand?" without anyone guessing.
  </p>

  <p class="story-text">
    But every customer conversation I've had eventually circles to the same complaint: <em>"our cleaning vendor is the worst part of our day."</em> Different cleaner every visit. Verbal quotes that don't hold. Photos? "We'll send them when we can." Damage? "We'll look into it." A subscription nobody can leave.
  </p>

  <p class="story-text">
    So I started keeping a list. By the time I hit <strong>seven recurring failure patterns</strong>, I was no longer auditing cleaning vendors. I was sketching what one would look like if it ran with the same operational rigor my customers expect from their software — and what I'd been failing to find for my own home in Calgary.
  </p>

  <p class="story-text">
    Elite Clean Pro is what those notes became. The 100-point checklist exists because nobody publishes one. The post-clean quality report exists because every other "report" is verbal and self-serving. The named account contact exists because I've spent my career being the named account contact in software — and I know exactly how much difference that makes versus a shared inbox.
  </p>

  <p class="story-text">
    We're new. We don't have a decade of customer history in Calgary. <em>What we're bringing is the operational discipline I run in PropTech sales every day — documented, measurable, accountable — applied to a service that hasn't had it yet.</em>
  </p>
</section>

<!-- ════════ SEVEN OBSERVATIONS ════════ -->
<section class="observations">
  <div class="section-head-2col">
    <div class="section-num">02 / <span class="teal">Origin</span></div>
    <div>
      <h2 class="section-title">Seven patterns. <em>Repeated everywhere.</em></h2>
      <p class="section-lede">Across hundreds of conversations with the landlords and property managers I sell PropTech to — and a year of trying to hire a cleaner for my own home in Calgary — the same seven operational failures kept appearing. These are them. The Standard was built to address all seven.</p>
    </div>
  </div>

  <div class="obs-list">

    <div class="obs">
      <div class="obs-num">01</div>
      <div class="obs-body">
        <h3 class="obs-title">"Deep clean" meant something different at every company.</h3>
        <p class="obs-text">Same word, three different scopes, three different prices. No published list anywhere.</p>
      </div>
    </div>

    <div class="obs">
      <div class="obs-num">02</div>
      <div class="obs-body">
        <h3 class="obs-title">Quotes were verbal estimates, not contracts.</h3>
        <p class="obs-text">Final invoices regularly arrived 15–40% higher than the quote, with "additional time" as the only line item.</p>
      </div>
    </div>

    <div class="obs">
      <div class="obs-num">03</div>
      <div class="obs-body">
        <h3 class="obs-title">A different cleaner every visit.</h3>
        <p class="obs-text">No consistency, no familiarity with the space, no accountability. Every visit was effectively a first visit.</p>
      </div>
    </div>

    <div class="obs">
      <div class="obs-num">04</div>
      <div class="obs-body">
        <h3 class="obs-title">Communication was a shared inbox.</h3>
        <p class="obs-text">Writing to "info@" or "support@" produced form replies signed "the team." No named human, ever.</p>
      </div>
    </div>

    <div class="obs">
      <div class="obs-num">05</div>
      <div class="obs-body">
        <h3 class="obs-title">Subscriptions were traps.</h3>
        <p class="obs-text">$0 to sign up, $250 to cancel. Penalties for skipping visits. Auto-renewal hidden in the fine print.</p>
      </div>
    </div>

    <div class="obs">
      <div class="obs-num">06</div>
      <div class="obs-body">
        <h3 class="obs-title">Damage protocols were "we'll look into it."</h3>
        <p class="obs-text">No written process for what happens when something breaks. No published outcomes. No accountability timeline.</p>
      </div>
    </div>

    <div class="obs">
      <div class="obs-num">07</div>
      <div class="obs-body">
        <h3 class="obs-title">Quality reviews were self-graded surveys.</h3>
        <p class="obs-text">"Rate your experience 1–5." No checklist scoring. No photo evidence. No data the customer could act on.</p>
      </div>
    </div>

  </div>

  <p class="obs-link">
    Each pattern got a documented response.
    <a href="/the-standard">Read The Standard →</a>
  </p>
</section>

<!-- ════════ PHOTO SECTION — PLACEHOLDER ════════ -->
<section class="photos" id="photos">
  <div class="section-head-2col">
    <div class="section-num">03 / <span class="teal">The people</span></div>
    <div>
      <h2 class="section-title">The face behind <em>the system.</em></h2>
      <p class="section-lede">Cleaning is human work. The system is only ever as good as the team running it. Photos coming soon — until then, this is the layout we've designed for them.</p>
    </div>
  </div>

  <div class="photos-grid">

    <div class="photo-slot large">
      <div class="photo-slot-tag">[ Photo 01 — primary ]</div>
      <p class="photo-slot-label">Founder portrait, or workspace overview, or team-at-work hero shot.</p>
      <p class="photo-slot-spec">Recommended · 4:5 portrait or 3:4 · Min 1200 × 1500 px</p>
    </div>

    <div class="photo-slot">
      <div class="photo-slot-tag">[ Photo 02 ]</div>
      <p class="photo-slot-label">Team in uniform — the A+ crew that runs the visits.</p>
      <p class="photo-slot-spec">Recommended · 4:3 landscape · Min 1000 × 750 px</p>
    </div>

    <div class="photo-slot">
      <div class="photo-slot-tag">[ Photo 03 ]</div>
      <p class="photo-slot-label">BioSmart system — equipment, vacuums, color-coded kit.</p>
      <p class="photo-slot-spec">Recommended · 4:3 landscape · Min 1000 × 750 px</p>
    </div>

  </div>

  <p class="photos-note">[ Replace placeholders with real photos before launch · Editorial · No stock images ]</p>
</section>

<!-- ════════ WHAT WE BELIEVE ════════ -->
<section class="beliefs">
  <div class="section-head-2col">
    <div class="section-num">04 / <span class="teal">Operating principles</span></div>
    <div>
      <h2 class="section-title">What we <em>actually believe.</em></h2>
      <p class="section-lede">Not aspirational values. Operational principles. Things that show up in how every visit runs, every email gets answered, every report gets written.</p>
    </div>
  </div>

  <div class="beliefs-list">

    <div class="belief">
      <div class="belief-left">
        <div class="belief-num">Principle 01</div>
        <h3 class="belief-name">Document the <em>scope.</em></h3>
      </div>
      <p class="belief-body">If we can't write down what's in scope and what isn't, the scope isn't real. Every service has a published checklist. Every clean is measured against it. <strong>"Trust us" is not a deliverable.</strong></p>
    </div>

    <div class="belief">
      <div class="belief-left">
        <div class="belief-num">Principle 02</div>
        <h3 class="belief-name">Publish the <em>flaws.</em></h3>
      </div>
      <p class="belief-body">Every page on this site says what we don't do, where we won't go, and what we charge. Honest disclosure builds more trust than perfect-sounding copy. <strong>If we hide it, we'd have to defend it.</strong></p>
    </div>

    <div class="belief">
      <div class="belief-left">
        <div class="belief-num">Principle 03</div>
        <h3 class="belief-name">A name on <em>every account.</em></h3>
      </div>
      <p class="belief-body">No shared inboxes. No ticket queues. Every customer relationship has one named human assigned to it, with a direct email and a direct phone line. <strong>The person who runs your account is a person, not a brand.</strong></p>
    </div>

    <div class="belief">
      <div class="belief-left">
        <div class="belief-num">Principle 04</div>
        <h3 class="belief-name">Measure the <em>output.</em></h3>
      </div>
      <p class="belief-body">Every clean gets scored against the 100-point checklist system. Photo evidence per room. Written report within an hour. <strong>If we can't measure the work, we can't tell you whether we did it.</strong></p>
    </div>

    <div class="belief">
      <div class="belief-left">
        <div class="belief-num">Principle 05</div>
        <h3 class="belief-name">Cancel <em>any time.</em></h3>
      </div>
      <p class="belief-body">Recurring service is a recurring slot we hold for you, not a debt you owe us. No subscription locks. No cancellation traps. <strong>If we have to trap you to keep you, we shouldn't have you.</strong></p>
    </div>

    <div class="belief">
      <div class="belief-left">
        <div class="belief-num">Principle 06</div>
        <h3 class="belief-name">Edit, <em>don't add.</em></h3>
      </div>
      <p class="belief-body">Every operational decision starts with "can this be simpler?" Every customer-facing page gets shorter over time, not longer. <strong>Discipline shows up in what gets cut, not in what gets added.</strong></p>
    </div>

  </div>
</section>

<!-- ════════ WHERE WE ARE NOW — pre-launch honest ════════ -->
<section class="current" id="now">
  <div class="current-eyebrow">Where we are right now</div>
  <h2 class="current-title">A system, <em>ready to run.</em></h2>
  <p class="current-text">
    We launched in 2026. We don't yet have a decade of testimonials or thousands of audited cleans. <em>What we have is the system — published, documented, defensible — applied carefully on every visit.</em> The reviews will come from the work. The work will come from the system. The system is here, in plain English, before the customers are.
  </p>
  <p class="current-text">
    Why publish before we have customers? Because the system is what differentiates us. Anyone can write nice copy after the fact. <em>Almost nobody publishes the operations beforehand and then has to live up to them.</em>
  </p>

  <div class="current-strip">
    <div class="current-stat">
      <div class="current-stat-label">Founded</div>
      <div class="current-stat-value"><span class="teal">2026</span></div>
    </div>
    <div class="current-stat">
      <div class="current-stat-label">Based in</div>
      <div class="current-stat-value">Calgary, <span class="teal">AB</span></div>
    </div>
    <div class="current-stat">
      <div class="current-stat-label">System pages published</div>
      <div class="current-stat-value"><span class="teal">9</span></div>
    </div>
    <div class="current-stat">
      <div class="current-stat-label">Booking</div>
      <div class="current-stat-value"><span class="teal">Open</span></div>
    </div>
  </div>
</section>

<!-- ════════ CTA ════════ -->
<section class="cta">
  <div class="cta-eyebrow">The contract is already <span style="color:var(--teal)">signed</span></div>
  <h2 class="cta-title">Book the clean. <em>Hold us to the system.</em></h2>
  <p class="cta-sub">Get an instant quote. Same-week availability across most of Calgary. The system you've read about runs the moment you book — exactly as published.</p>
  <div class="cta-actions">
    <a href="/#quote" class="btn btn-primary">Get instant quote →</a>
    <a href="/the-standard" class="btn btn-ghost">Read the Standard</a>
  </div>
</section>

<!-- ════════ FOOTER ════════ -->
