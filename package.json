{
  "name": "elite-clean-pro",
  "version": "1.0.0",
  "description": "Elite Clean Pro — Calgary cleaning service marketing site",
  "scripts": {
    "build": "NODE_ENV=production eleventy && NODE_ENV=production node scripts/minify-assets.js",
    "serve": "eleventy --serve",
    "dev": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.47.1"
  },
  "private": true
}
